import styled from 'styled-components/native';

/**
 * Component for wrapping another component with an app style.
 * @return {View} Returns styled View which flex is set to 1 and background color.
 */

const Container = styled.View`
  flex: 1;
  background-color: #fff;
`;

export default Container;
